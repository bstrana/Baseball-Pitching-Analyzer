The app is a static, client-side tool — no database or account setup is required.

Open the app URL and grant camera access when your browser prompts for it
(pose tracking runs entirely in-browser; nothing is uploaded to the server).

This app has no built-in login. If you want to restrict who can reach it, use
Cloudron's app access control (App -> Users tab) to limit it to specific users
or groups.
