# Baseball Mechanics Analyzer

Real-time baseball movement analysis using TensorFlow.js for tracking pitcher mechanics and swing paths.

## Run Locally

**Prerequisites:** Node.js

1. Install dependencies:
   `npm install`
2. Run the app:
   `npm run dev`
